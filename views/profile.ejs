<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    
    <title>Profile Page !!</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-800 text-white p-10">
        <div class=" w-full flex justify-end ">
            <a href="/logout" class=" px-3 py-2 bg-red-600 rounded-md text-sm mb-5 inline-block "   > logout </a>      
        </div>
        <h4>Profile pic </h4>
        <h3 class=" text-3xl mb-5"> <span>Hello <%= user.name %> 👋👋</span></h3>
         <h5 class="mb-5"> you can create a new post </h5>
         <form action="/upload " method="post">
            <textarea placeholder="what's on your mind?" class= " w-1/3  block resize-none bg-transparent bordeer-2 border-zinc-800 rounded-md " name="content" ></textarea>
             <input type="submit" class=" px-3 w-40 py-2 bg-blue-500 block rounded-md mt-3" value="Create new post"> 
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400"> Your Post </h3>
           
            <div class="postcontainer mt-5">
                
                <% user.posts.reverse().forEach(function(post){%>
                    <div class="post w-1/3 p-4 mb-5 rounded-md bg-zinc-800 border-zinc-700 border-2">
                        <div class="flex justify-between mb-3">
                            <h4 class="text-blue-500">@<%= user.username %></h4>
                            <h1><%= post.date %></h1>
                        </div>
                        <p class="text-sm tracking-tight"><%= post.content %></p>
                        <small class="mt-6 inline-block"><%= post.likes.length %></small>
                        <div class="btns flex mt-1 gap-4 ">
                            <a class="text-blue-500" href="/like/<%= post._id%>">Like</a>
                            <a class="text-zinc-600" href="/edit/<%= post._id %>">Edit</a>
                        </div>
                    </div>
                    
                    <% }) %>
                
            </div>
        </div>
    </div>
</body>
</html>
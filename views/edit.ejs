<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Edit post</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-800 text-white p-10">
        <div class=" w-full flex justify-end ">
            <a href="/logout" class=" px-3 py-2 bg-red-600 rounded-md text-sm mb-5 inline-block "   > logout </a>      
        </div>
        <h1 class="mb-5 text-3xl "> Edit Your content </h1>
         <h5 class="mb-5"> Edit your post  </h5>
         <form action="/update/<%= post._id %> " method="post">
            <textarea class= " w-1/3  block resize-none bg-transparent bordeer-2 border-zinc-800 rounded-md " name="content" ><%= post.content %></textarea>
             <input type="submit" class=" px-3 w-40 py-2 bg-blue-500 block rounded-md mt-3" value="Save"> 
        </form>
    </div>
</body>
</html>